version: "3.9"

services:
  app:
    build: .
    container_name: fastapi_app
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_started
    environment:
      - DATABASE_URL=postgresql://test_pg_1vk2_user:NlohnDw5va25sEEtwYzaQv6eMbBLckwm@dpg-cufm5ai3esus73e27ps0-a.oregon-postgres.render.com/test_pg_1vk2

  db:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_DB: test_pg_1vk2
      POSTGRES_USER: test_pg_1vk2_user
      POSTGRES_PASSWORD: NlohnDw5va25sEEtwYzaQv6eMbBLckwm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
